/*Q5.Accept two integers from the user and 
interchange them. Display the interchanged numbers.*/
#include<stdio.h>
int main()
{
	int a,b;
	printf("Enter the two numbers a and b :");
	scanf("%d%d",&a,&b);
	printf("The interchanged numbers are a=%d and b=%d",b,a);
	return 0;
}
